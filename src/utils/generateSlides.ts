export default function generateSlides() {}

// export function generateSlides(
//   length = 4,
//   sig = 0
// ): Array<{ src: string; alt: string }> {
//   return Array.from({ length }).map((value, index) => {
//     index = sig || index;

//     return {
//       src: `/assets/images/ss/${index}.png`,
//       alt: `Image ${index + 1}`,
//     };
//   });
// }
